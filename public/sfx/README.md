# Sound effects

Language-independent (PRD.md §4.1). Referenced by name from `src/book.ts`.

| File | Used on |
|---|---|
| `chime.mp3` | p1 — stars twinkle |
| `rustle.mp3` | p2 — costume |
| `sparkle.mp3` | p3 — costume lights up |
| `creak.mp3` | p4 — door opens |
| `knock.mp3` | p5 / p7 / p9 — knocking |
| `boop.mp3` | p6 / p8 / p10 — sticker onto the bag |
| `count.mp3` | p11 — per-sticker blip (spoken numbers come from `audio/<lang>/count/`) |
| `lullaby.mp3` | p12 — bedtime |

Keep them short (< 1.5 s), soft, non-startling.
